#include "main.h"
#include <stdio.h>

/**
 * _isupper - function shows the upper
 * @c: is caracter
 * Return: Always 0.
 */
int _isupper(int c)

{

	if (c >= 65 && c <= 90)
	{
	return (0);
	}
	return (1);
}
