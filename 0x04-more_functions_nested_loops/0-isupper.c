#include "main.h"
#include <stdio.h>

/**
 * main - check the code.
 * @c: is caracter
 * Return: Always 0.
 */
int _isupper(int c);
{
	char c;

	for (c = 'a'; c >= 'z'; c++)
	if(l == c) 
	{ 
	return (0);
	}
	else
	{
	return (1)
	}
}
