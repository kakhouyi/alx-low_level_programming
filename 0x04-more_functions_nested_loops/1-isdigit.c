#include "main.h"
#include <stdio.h>

/**
 * _isdigit - function shows the upper
 * @c: is caracter
 * Return: Always 0.
 */
int _isdigit(int c)

{

	if (c >= 48 && c <= 57)
	{
	return (1);
	}
	return (0);
}
