#include "main.h"

/**
* _islower - print
* @c: is The caracter to check
* Return:  1 for lowercase or 0 for anything
*/

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}
		return (0);
}
